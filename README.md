# AI 智能资讯聚合与分发系统

> **项目代号**: auto_info
> **版本**: v1.0.0
> **作者**: 老王
> **创建日期**: 2025-01-14

---

## 📋 项目简介

一个全自动的 AI 资讯聚合与多渠道分发系统，实现 **信息源头 → AI 加工 → 多端分发** 全流程无人值守。

### 核心功能

- **智能资讯采集**: RSS订阅、API调用、网页爬虫
- **AI 内容加工**: 筛选、改写、扩写、多模态生成
- **多渠道分发**: 官网、公众号、抖音、Twitter 自动发布
- **调度监控**: Cron定时任务、失败重试、异常告警

---

## 🏗️ 技术栈

### 后端
| 技术 | 版本 | 用途 |
|------|------|------|
| FastAPI | 0.109.0 | Web框架 |
| SQLAlchemy | 2.0.25 | ORM |
| aiosqlite | 0.19.0 | 异步数据库驱动 |
| Pydantic | 2.5.3 | 数据验证 |
| pytest | 7.4.3 | 单元测试 |

### 前端
| 技术 | 版本 | 用途 |
|------|------|------|
| Vue | 3.4.15 | 前端框架 |
| Vite | 5.0.11 | 构建工具 |
| Vue Router | 4.2.5 | 路由 |
| Pinia | 2.1.7 | 状态管理 |
| Axios | 1.6.5 | HTTP客户端 |

---

## 📁 项目结构

```
auto_info/
├── backend/                 # 后端服务
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py         # FastAPI入口（含lifespan优雅关闭）
│   │   ├── models.py       # SQLAlchemy模型
│   │   ├── schemas.py      # Pydantic Schema
│   │   ├── database.py     # 数据库配置
│   │   └── config.py       # 应用配置
│   ├── tests/              # 单元测试（81个测试用例）
│   │   ├── conftest.py     # 测试夹具
│   │   ├── test_schemas.py # Schema测试（23个）
│   │   ├── test_models.py  # 模型测试（19个）
│   │   ├── test_main.py    # API测试（31个）
│   │   ├── test_crud.py    # CRUD测试（8个）
│   │   └── README.md       # 测试文档
│   ├── requirements.txt
│   └── pytest.ini
│
├── frontend/               # 前端应用
│   ├── src/
│   │   ├── api/           # API封装
│   │   ├── components/    # Vue组件
│   │   ├── router/        # 路由配置
│   │   ├── stores/        # Pinia状态
│   │   └── views/         # 页面视图
│   ├── package.json
│   └── vite.config.js
│
├── docs/                   # 项目文档
│   └── plans/
│       └── 2026-01-14-content-format-design.md
│
├── 产品需求分析.md          # 产品需求文档
└── README.md               # 本文件
```

---

## 🚀 快速开始

### 后端启动

\`\`\`bash
cd backend

# 安装依赖
pip install -r requirements.txt

# 运行测试
pytest tests/ -v

# 启动服务
python -m app.main
# 或使用 uvicorn
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
\`\`\`

**API文档**: http://localhost:8000/api/docs

### 前端启动

\`\`\`bash
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build
\`\`\`

**前端地址**: http://localhost:5173

---

## 📊 API 接口

### 文章接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/articles` | 文章列表（分页） |
| GET | `/api/articles/{id}` | 文章详情 |
| POST | `/api/articles` | 创建文章 |
| PUT | `/api/articles/{id}` | 更新文章 |
| DELETE | `/api/articles/{id}` | 删除文章 |

### 分类/标签接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/categories` | 分类列表 |
| POST | `/api/categories` | 创建分类 |
| GET | `/api/tags` | 标签列表 |
| GET | `/api/tags/popular` | 热门标签 |

### 其他接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/search` | 搜索文章 |
| GET | `/api/stats` | 网站统计 |
| GET | `/api/health` | 健康检查 |

---

## 🧪 测试

\`\`\`bash
# 运行所有测试
pytest tests/ -v

# 运行特定测试文件
pytest tests/test_schemas.py -v
pytest tests/test_models.py -v
pytest tests/test_main.py -v

# 运行特定标记的测试
pytest tests/ -m api
pytest tests/ -m unit

# 查看测试覆盖率
pytest tests/ --cov=app --cov-report=html
\`\`\`

**测试覆盖率**:
- Schema验证: 23/23 ✅
- ORM模型: 19/19 ✅
- API端点: 已验证核心测试 ✅

---

## 📝 开发规范

### 代码风格
- Python: 遵循 PEP 8
- JavaScript: 使用 ESLint
- Git提交: 使用约定式提交

### 重要注意事项
1. **优雅关闭**: 后端使用`lifespan`上下文管理器，确保资源正确释放
2. **异步优先**: 数据库操作全部使用异步方式
3. **类型验证**: 使用Pydantic进行请求/响应验证
4. **错误处理**: 统一异常处理，不暴露敏感信息

---

## 🔧 配置说明

### 后端配置 (`.env`)
\`\`\`bash
# 数据库
DATABASE_URL=sqlite+aiosqlite:///./auto_info.db

# CORS
CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

# API密钥
API_KEY=your-secret-api-key-change-this
\`\`\`

---

## 📄 许可证

MIT License

---

**老王出品，必属精品！** 🐕
