# 前端产品优化设计方案

> 创建日期：2025-01-15
> 设计者：老王
> 项目：AI智能资讯聚合系统

---

## 一、需求背景

### 核心目标
- **交互体验**：快速搜索、相关文章推荐、一键社交分享
- **多端自适应**：完美适配 PC、平板及移动端，手机端极致阅读体验

### 用户场景
**双场景模式**：首页快速浏览 + 详情页深度阅读

---

## 二、整体架构设计

### 三层渐进式增强架构

```
┌─────────────────────────────────────────────────────────┐
│                    导航层（顶部常驻）                    │
│  AppHeader + 即时搜索框 + 防抖(300ms) + 实时建议下拉    │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                    内容层（核心体验）                    │
│  首页：瀑布流卡片 + Hero轮播（快速浏览）                │
│  详情页：专注阅读区 + 侧边推荐栏（深度阅读）             │
│  字体流式适配：clamp() 实现自动响应                     │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                    交互层（增强功能）                    │
│  分享组件：抽屉式/底部弹出                              │
│  推荐组件：智能混排算法                                 │
│  阅读设置：悬浮工具栏（字号/暗黑/进度）                 │
└─────────────────────────────────────────────────────────┘
```

### 技术栈
- Vue 3 + Pinia + Vue Router
- 复用现有组件，遵循 KISS、DRY、SOLID 原则

---

## 三、核心组件设计

### 1. InstantSearch（即时搜索组件）

**路径**：`components/shared/InstantSearch.vue`

**功能**：
- 输入框 + 防抖（300ms）
- 下拉建议框：展示5条热门匹配
- 支持键盘 ↑↓ 选择 / Enter 跳转
- 移动端：全屏搜索模态框

**UI布局**：

| 端 | 搜索UI位置 |
|---|-----------|
| PC | AppHeader 右侧常驻搜索框，输入时下方显示建议 |
| 移动 | 搜索图标 → 全屏搜索模态框 |

```
PC端布局：
┌─────────────────────────────────────────────────────┐
│  Logo    分类1 | 分类2 | 分类3    [🔍 搜索框...]      │
└─────────────────────────────────────────────────────┘
                                        ↓
                         ┌──────────────────┐
                         │ 🔍 AI            │
                         │ 🔍 ChatGPT       │
                         └──────────────────┘

移动端布局：
点击搜索图标 → 全屏模态框
┌───────────────────┐
│  [×] 取消         │
│  ┌─────────────┐ │
│  │ 🔍 搜索...  │ │
│  └─────────────┘ │
│  🔍 AI           │
│  🔍 ChatGPT      │
└───────────────────┘
```

---

### 2. ArticleRecommend（相关推荐组件）

**路径**：`components/article/ArticleRecommend.vue`

**功能**：
- 三层混排逻辑：
  - 同标签文章（60%权重）
  - 同分类热门（30%权重）
  - 最近阅读历史补充（10%权重）
- 智能去重：已读文章标记
- 移动端：横向滑动卡片

**推荐算法**：
```js
// 1. 同标签优先
const byTag = articles.filter(a => hasCommonTag(a.tags, currentTags))

// 2. 同分类热门兜底
const byCategory = await fetchHotArticles(currentCategory)

// 3. 阅读历史补充
const fromHistory = getReadHistory().slice(0, 2)

// 混排去重，取前6条
const recommended = mixAndDedupe(byTag, byCategory, fromHistory).slice(0, 6)
```

---

### 3. ShareDrawer（分享抽屉组件）

**路径**：`components/shared/ShareDrawer.vue`

**功能**：
- PC：右侧滑出
- 移动：底部弹出
- 平台按钮：复制链接 / 微信二维码 / 微博 / 小红书
- 二维码：qrcode.vue 生成

**UI布局**：

| 端 | 分享UI位置 |
|---|-----------|
| PC | 文章详情页右上角按钮 → 右侧滑出抽屉 |
| 移动 | 文章详情页右上角按钮 → 底部弹出 |

```
PC端：
点击分享 → 右侧滑出
┌────────────┐
│  分享到    │
│            │
│  📋 复制   │
│  💬 微信   │
│  📱 微博   │
│  📕 小红书 │
└────────────┘

移动端：
点击分享 → 底部弹出
┌─────────────────┐
│  取消   分享    │
├─────────────────┤
│ 📋 复制链接     │
│ 💬 分享到微信   │
│ 📱 分享到微博   │
│ 📕 分享到小红书 │
└─────────────────┘
```

**分享URL示例**：
- 微博：`https://service.weibo.com/share/share.php?url={url}&title={title}`
- 小红书：需用户手动复制链接（小红书API限制）

---

### 4. ReadingToolbar（阅读工具栏）

**路径**：`components/article/ReadingToolbar.vue`

**功能**：
- 字号缩放：A- / A+ / 默认（14px~20px范围）
- 阅读进度：顶部细线进度条
- 暗黑模式切换
- 移动端：悬浮球（点击展开）

---

## 四、移动端阅读舒适度实现

### 流式排版系统

```scss
// 基础字号：手机14px ~ PC 18px
font-size: clamp(14px, 2.5vw, 18px);

// 标题层级
h1 { font-size: clamp(24px, 5vw, 32px); }
h2 { font-size: clamp(20px, 4vw, 28px); }
h3 { font-size: clamp(18px, 3vw, 24px); }

// 行高随字号动态调整
line-height: clamp(1.6, 1.8, 2.0);
```

### 内容区域宽度

```scss
.article-content {
  padding: 0 16px;        // 手机左右边距
  max-width: 800px;       // PC最大宽度
  margin: 0 auto;         // 居中
  width: 100%;
}
```

### 手势缩放功能

```js
// 三档字号
const fontSizes = {
  small: '14px',
  medium: '17px',  // 默认
  large: '20px'
}

// 应用到 content 容器
document.documentElement.style.setProperty(
  '--reading-font-size',
  fontSizes[level]
)
```

### 暗黑模式

```scss
:root {
  --bg-primary: #ffffff;
  --text-primary: #1a1a1a;
}

@media (prefers-color-scheme: dark) {
  :root {
    --bg-primary: #1a1a1a;
    --text-primary: #e0e0e0;
  }
}

.dark-mode {
  --bg-primary: #1a1a1a;
  --text-primary: #e0e0e0;
}
```

### 阅读进度条

```scss
.reading-progress {
  position: fixed;
  top: 60px;  // header下方
  left: 0;
  height: 2px;
  background: var(--primary-color);
  width: 0%;
  transition: width 0.1s;
  z-index: 100;
}
```

---

## 五、数据流设计

### 搜索数据流

```
用户输入
    ↓
debounce(300ms)
    ↓
前端筛选本地缓存 / 后端API
    ↓
GET /api/articles/search?q=关键词&limit=5
    ↓
下拉建议框展示
```

### 推荐数据流

```
文章详情页加载
    ↓
获取当前文章的 tags + category
    ↓
GET /api/articles/{id}/related
    ↓
前端三层混排：
  1. 同标签文章
  2. 同分类热门
  3. localStorage读历史记录
    ↓
ArticleRecommend 渲染
```

### 阅读历史存储

```js
// 存到 localStorage，限制50条
const saveReadHistory = (article) => {
  const history = JSON.parse(
    localStorage.getItem('readHistory') || '[]'
  )
  const filtered = history.filter(h => h.id !== article.id)
  filtered.unshift({
    id: article.id,
    title: article.title,
    readAt: Date.now()
  })
  localStorage.setItem(
    'readHistory',
    JSON.stringify(filtered.slice(0, 50))
  )
}
```

### 分享数据流

```
点击分享按钮
    ↓
生成当前文章 URL
    ↓
┌─────────────┬──────────────────┬────────────────┐
│ 微信二维码   │ 复制链接         │ 微博/小红书    │
│ qrcode.vue  │ clipboard API    │ 打开分享URL    │
└─────────────┴──────────────────┴────────────────┘
```

---

## 六、后端API需求

### 需要新增的API

| 接口 | 方法 | 说明 |
|-----|------|-----|
| `/api/articles/search` | GET | 搜索文章（q=关键词&limit=5） |
| `/api/articles/{id}/related` | GET | 获取相关推荐文章 |
| `/api/categories/{slug}/hot` | GET | 分类热门文章（limit=5） |

### API 响应格式

```json
// GET /api/articles/search?q=AI&limit=5
{
  "code": 0,
  "message": "success",
  "data": [
    { "id": 1, "title": "AI大模型...", "slug": "ai-model", "excerpt": "..." },
    { "id": 2, "title": "深度学习...", "slug": "deep-learning", "excerpt": "..." }
  ]
}

// GET /api/articles/{id}/related
{
  "code": 0,
  "message": "success",
  "data": {
    "by_tag": [...],      // 同标签推荐
    "by_category": [...], // 同分类热门
    "from_history": [...] // 历史记录推荐（前端计算）
  }
}
```

---

## 七、实现清单

| 功能 | 新建组件 | 修改文件 | 后端API | 优先级 |
|------|---------|---------|---------|-------|
| 即时搜索 | InstantSearch.vue | AppHeader.vue | /api/articles/search | P0 |
| 相关推荐 | ArticleRecommend.vue | ArticleDetailView.vue | /api/articles/{id}/related | P0 |
| 分享功能 | ShareDrawer.vue | ArticleDetailView.vue | 无 | P1 |
| 阅读工具 | ReadingToolbar.vue | ArticleDetailView.vue | 无 | P1 |
| 暗黑模式 | - | 全局样式 | 无 | P2 |
| 阅读进度 | - | ArticleDetailView.vue | 无 | P2 |

### 需要安装的依赖

```bash
npm install qrcode @vueuse/core
```

---

## 八、设计原则

- **KISS**：组件职责单一，避免过度抽象
- **DRY**：复用现有 ArticleCard、ArticleGrid 组件
- **SOLID**：
  - S：每个组件只做一件事
  - O：支持未来扩展更多分享平台
  - L：保持组件接口一致
  - I：组件Props精简，不传冗余参数
  - D：依赖 Pinia Store 状态管理

---

## 九、验收标准

### 即时搜索
- [ ] 输入300ms后显示建议
- [ ] 支持键盘上下选择
- [ ] 移动端全屏搜索正常

### 相关推荐
- [ ] 显示6条推荐文章
- [ ] 已读文章有标记
- [ ] 移动端横向滑动流畅

### 分享功能
- [ ] 复制链接成功提示
- [ ] 微信二维码正确显示
- [ ] 微博分享链接可打开

### 阅读体验
- [ ] 字号A+/A-功能正常
- [ ] 暗黑模式切换生效
- [ ] 阅读进度条准确

---

*设计文档版本：v1.0*
*最后更新：2025-01-15*
